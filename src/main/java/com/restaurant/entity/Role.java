package com.restaurant.entity;

/**
 * Enum representing the role assigned to an application user.
 * Maps to the 'Role' ENUM column in the AppUser table.
 */
public enum Role {
    CUSTOMER,
    ADMIN
}
